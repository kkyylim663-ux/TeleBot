# 账单明细页面（只读记录页）· 业务说明书

> 本文档是「📋 账单明细」网页的**唯一定义文档**。页面由 `webconsole.py` 内嵌在 Bot 进程里提供，
> 手机从 Telegram 账单卡片的「📋 账单明细」按钮直达（线上 `https://pg.count168.site/`）。
>
> 定位：**只读账单记录页**——它只负责「看」，不负责「记」。记账一律在 Telegram 群里完成。

---

## 一、页面结构（从上到下）

```
┌ ‹  账单明细                        [EN] [🌙] [AUD] ┐
│    Bill Details（右上角：语言 / 一键昼夜 / 币种）    │
├ 日期卡：📅 2026-09-15 → 2026-09-23        ✕         │
│         时间（可选）[00:00] 至 [23:59]               │
├ 入账卡：🟩↓ 入账 6笔                    总计 +450     │
│   表：时间 / 金额 / 标记 / 操作人 / 备注              │
├ 下发卡：🟪⇩ 下发 2笔                   总计 -660     │
├ 分组卡：🟦▦ 分组 1组                   总计 -30       │
│   表：时间 / 代号 / 总入金额 / 总出金额 / 总账         │
├ 总计卡：🟦▤ 总计                                     │
│   [总入金额 150] [总出金额 660] [总账金额 -510]       │
├ 顶栏右侧：语言 / 一键昼夜 / 导出 PDF（图标按钮，走系统打印）│
└ 页脚：网页与 Telegram 共用同一份账本 · 只读查阅        ┘
```

外观遵循设计稿：顶栏无底色大条（返回箭头 + 中文主标题 + 英文副标题 + 右上角「语言 / 昼夜分段开关 / 币种」），卡片圆角 16px、卡片头左侧色块图标、右侧该表「总计」，表头行浅色底 + 圆角，底部「总计」独立成卡。

- **大标题**固定为「账单明细」（英文 Ledger Detail）。**没有副标题行**（群名/账期都不显示：下面已经有日期了）。
- **大标题右边是币种徽章**（如 `AUD`）：**只展示，不可切换**。一个群的账本就是单一币种 —— 换币种是在 Telegram 里发「修改币种 AUD到MYR」，那条指令会把整个账单（所有未撤销记录 + 结转 + 设置）一起换掉，所以网页不需要、也不应该提供切换。
  - 页面显示的是该群**当前设置的币种**；正常情况整本同币种，行内不重复标币种。
  - 例外：如果历史上用「设置币种 X」（只改后续记录）而不是「修改币种」换过币种，会出现异币种的老记录。这类记录**不会被隐藏**（与 Telegram 账单卡片口径一致，卡片也不按币种过滤），只会在金额后**单独标注它是哪个币种**，避免金额被误读。
- **右上角两个切换**：`🌙/☀️` 夜间模式、`EN/中文` 中英切换，两者都用 `localStorage` 记住选择。
- **没有任何记账入口**：页面不提供「记一笔」「入账/出账」按钮，也不提供扫描单确认。旧的「快速记账」「汇总」「待确认账单」三个区块已按需求整体移除。

---

## 二、区间：第一排日期（一点进去选）、第二排时间（可选）

页面顶部就两排，没有别的开关：

```
第一排   📅  2026-09-15   →   2026-09-23            ✕  ▾
第二排   时间（可选）  [00:00]  至  [23:59]
```

### 第一排 · 日期区间（一个控件，点一次就进去选）

- 整排是**一个按钮**，不拆成两个输入框。点一下 → 弹出日历。
- 日历里**点两下就把区间圈好**：第一下设为开始日期，提示随即变成「再点一下结束日期」，第二下设为结束日期。只点一下也行（表示"从这天起"）。
- 面板里只有日历和月份切换，没有额外选项；「完成」收起面板，**每次点击都已即时生效**。
- 按钮右侧 **✕** 清空日期 = 不按日期筛选（这时第二排时间自动变灰不可用）。
- 历史账期时这个按钮置灰（归档只有汇总，没有明细可筛）。

### 第二排 · 时间（可选）

- 两个时间输入，默认 `00:00` 至 `23:59`，**不填就是整天**——绝大多数场景不用碰它。
- 需要精确到某个时刻时才改：例如日期选 09-15，时间开始填 `14:00`，就只看当天 14:00 之后的记录。
- 日期清空时这一排自动置灰（没有日期，时间没有意义）。

**最终筛选条件** = `开始日期 + 开始时间` 到 `结束日期 + 结束时间`；两端各自为空表示该端不限。改动立即刷新三张表（防抖 260ms）。

---

## 三、三张表的字段与口径

三张表的数字**同源**：都基于「当前账期 + 当前日期区间」这一份筛选结果计算，保证页面上看到的数字互相自洽。

### 表一 · 入账
| 列 | 来源 | 说明 |
|---|---|---|
| 时间 | `time` | 显示 `MM-DD HH:MM`，账本里存的是群本地时区的时间字符串 |
| 金额 | `net_amount` | `+金额` 显示为正、`-金额` 显示为负（与 Telegram 卡片「已入账」同口径） |
| 标记 | `reply_user_name` | **被回复人**；没有回复任何人时显示 `—` |
| 操作人 | `operator_name` | 发消息的人（`@用户名` 或昵称） |
| 备注 | `note` | 记账时写在金额后面的文字 |

入账表包含 `+`（入账）与 `-`（出账）两种记录，按时间正序排列。被撤销的记录仍然显示，但**整行置灰 + 删除线**。

### 表二 · 下发
列与入账表相同，只包含 `下发` 记录。金额取 `net_amount`：

- `下发 2000 手续20` → 金额显示 `-1980`
- 冲正 `下发 +2000` → 金额显示 `+2000`，并在金额后追加「冲正」小字标签

### 表三 · 分组
**一行 = 一个代号**（Telegram 里 `KY +50 备注` 那个 KY）。

| 列 | 口径 |
|---|---|
| 时间 | 该代号**最近一笔**记录的时间 |
| 代号 | `entry["group"]`；无代号的下发/记账不进此表 |
| 总入金额 | 该代号 `+金额` 记录的**原始金额**之和（不扣手续费） |
| 总出金额 | 该代号 `-金额` 的原始金额之和 **+ 该代号下发的净流出**（已扣手续费；冲正会回冲成负贡献） |
| 总账 | 总入金额 − 总出金额 |

- 只统计**未撤销**的记录。
- Telegram 卡片里的「分组统计」是同一份口径的净额版（`代号 👉 净额`），只算 `+/-` 记账、不算下发；网页版按需求把下发也纳入代号，并拆成「总入 / 总出」两列。
- 分组卡的卡头「总计」= **各组总账之和**（分组口径的合计）。
- **已撤销（voided）的记录不在页面上显示**，也不计入任何数字（与 Telegram 账单卡片、日切口径一致）。
  有撤销记录时，该段标题会追加一句提示，例如「入账 5 笔（另有 1 笔已撤销） 总计 +1000」，
  让人知道为什么笔数比印象里少；提示本身不是记录，不需要可以去掉。
- 账期日切后明细会被清空，页面只展示当前账期的记录（归档汇总请用 Telegram 的「全局账单 MM-DD」查看）。

---

## 四、「标记」列 = 被回复人（新增字段）

### 业务含义
操作员**回复群里某个真人的消息**，然后发 `+100`，这条流水就会记下被回复人：

```
客户A：我转账了
操作员：（回复客户A那条消息）→ +100 已收
        ↑ 该笔的「标记」列显示 @客户A
```

### 记录规则（`bot.py: extract_reply_target()`）
| 场景 | 是否记录 |
|---|---|
| 回复**其他真人**的消息后记账 | ✅ 记 `reply_user_id` / `reply_user_name` |
| 回复 **Bot 自己**的消息（如记账卡片） | ❌ 不记（避免标记列全是机器人） |
| 回复**自己**的消息 | ❌ 不记 |
| **没有回复**，直接发 `+100` | ❌ 不记，标记列显示 `—` |

### 重要限制
- **只对新记录生效**：历史上已有的流水没有这个字段，标记列显示 `—`，不做回填。
- `+金额`、`-金额`、`下发` 三种记账都会记录被回复人。
- 该字段**不参与**任何统计，也不影响「撤销」的匹配逻辑（撤销仍按消息 ID 匹配被回复的那条记账消息）。

---

## 五、下发支持代号（写法变更）

| 写法 | 结果 |
|---|---|
| `下发 2000` | 旧写法，仍然可用；无代号 |
| `下发 2000 手续20 备注` | 旧写法，仍然可用 |
| `KY 下发 2000` | **新增**：该下发记入代号 KY，出现在分组表 |
| `KY 下发 2000 手续20 备注` | 新增：代号 + 手续费 + 备注 |
| `KY 下发 +2000` | 新增：代号 + 冲正 |

- 正则 `RE_LEDGER_DISBURSE`（`bot.py`）已改为可选代号前缀。
- 代号规则与 `KY +50` 保持一致：不含空白与 `+`/`-` 的一段文字。
- 守卫逻辑保持：`下发` 关键字不会被当代号（发 `下发 +2000` 仍走下发分支）。

---

## 六、账期与历史数据（硬约束）

| 概念 | 规则 |
|---|---|
| 当前账期 | 由 `period_start` 界定（`ledger_settings.json`），页面默认展示它的全部记录 |
| 日切（结束账单） | **会把当期流水物理清空**，同时把汇总写入 `global_bill_archive.json`（结算额/总进/总出/笔数/币种） |
| 历史账期 | **只有当日汇总，没有明细**——这是 Bot 的设计，不是页面缺功能。页面用一行归档数据表示 |

> 因此：页面上「开始/结束」区间能查的明细，**只存在于当前账期内**。

---

## 六点五、界面标准（可访问性，按 HIG/WCAG 校准）

页面所有文本色与控件尺寸都按下面的标准**实测**选定过，改动样式时请复核：

| 项 | 标准 | 本页实测 |
|---|---|---|
| 正文/小字对比度 | <18pt 需 ≥4.5:1 | 绿 5.14、红 5.58、次要灰 4.99~5.65、表头 5.65（浅色）；夜间模式全部 ≥5.2 |
| 最小字号 | 移动端 ≥11pt | 全页最小 11px（币种后缀/总计小标题） |
| 点击目标 | 移动端推荐 44×44，最小 28×28 | 日期控件 44、清除 44×44、夜间/中英 44×44、时间输入 44 高、日历格 47×47、关闭/翻月 44×44 |
| 颜色不是唯一线索 | 状态要有替代表达 | 入/出用 `+/-` 符号 + 颜色；已撤销用删除线 + 灰化 |
| 长表格扫读 | 斑马纹 | 表格偶数行浅底（浅色 `#f4f6f9` / 夜间 `#1b2740`）；斑马行上的金额对比度实测 5.15 / 5.36，仍 ≥4.5 |
| 加载反馈 | 请求期间要有状态 | 首屏三张表显示「加载中…」并置 `aria-busy` |
| 屏幕阅读器 | 控件需有可访问名称 | 图标按钮都带 `aria-label`；表头 `scope="col"`；错误提示 `role="alert"` |
| 安全区 | 刘海屏不被系统栏遮挡 | 顶栏 `padding-top: env(safe-area-inset-top)` |

### 底部「总计」卡片的口径

| 指标 | 算法 |
|---|---|
| **总入金额** | = 入账表的合计（含 `- 记一笔`，即净存入），已撤销不计 |
| **总出金额** | = 下发表净额取正数（下发金额已扣手续费，冲正会回冲），已撤销不计 |
| **总账金额** | = 总入金额 − 总出金额 |

> 入账表里 `-50` 这类「记一笔出账」本来就在入账表内（作为减项），下发是单独一张表；分组只是这些记录的拆分，所以这个口径**已经包含了分组**。页面、PDF、Excel 三处用的是同一套数字（已逐一对过）。

## 六点八、导出（PDF / Excel，用户自己选）

顶栏夜间模式右边是导出按钮（`⤓` 图标，44×44）。**点一下弹出两个选项**：

- **📄 导出 PDF** —— 走浏览器「打印 → 另存为 PDF」，内容见下（就是账单明细的干净版）。
- **📊 导出 Excel** —— 直接生成真正的 `.xlsx` 文件下载。

### 导出 PDF



导出按钮在**顶栏夜间模式按钮的右边**（`⤓` 图标，44×44，读屏标签「导出 PDF」）。它**不打印网页的卡片外观**（没有圆角卡片、彩色徽章），而是把当前账单明细按打印样式生成一份干净的文档，走浏览器自带的「打印 → 另存为 PDF」，不引入任何第三方库。

内容与页面完全一致（数字逐一核对过）：

```
账单明细
澳洲一号厅 · AUD · 2026-09-15 00:00 ~ 2026-09-23 23:59
────────────────────────────────────────────────
入账 6 笔                                  总计 +450
时间         金额    标记       操作人   备注
09-15 13:32   +200   —         John     —
…
────────────────────────────────────────────────
下发 2 笔                                  总计 -660
时间         金额              标记   操作人   备注
09-15 14:05   -480 (手续费 20)  —     John    下发 John
────────────────────────────────────────────────
分组 1 组                                    总计 -30
时间         代号   总入金额   总出金额   总账
09-15 16:20   KY     150        180       -30
════════════════════════════════════════════════
总计   总入金额 150   总出金额 660   总账金额 -510
导出时间 2026-09-23 22:04
```

- **不摆架子**：没有序号列、小计行、报告期间/有效笔数、作废统计等正式报表元素。
- **与页面数字一致**：每段「总计」= 页面上该卡片的合计；底部「总入金额/总出金额/总账金额」= 页面上「总计」卡片的值。
- **已撤销记录不列出、不计入**，只在末尾加一行说明（如「（另有 1 笔已撤销，未计入）」），Excel 则写在「总计」表里。
- 金额格式与页面一致（`+200` / `-50`），时间同页面（`MM-DD HH:MM`）。
- 打印样式：细线表 + 浅底表头 + 条目斑马纹、全黑白、`thead` 跨页重复、行不跨页断裂。
- 导出的是**当前筛选结果**（日期区间/时间生效中），文件名与内容都会带上期间，方便对账留痕。
- 文案跟随语言（中文导出中文，英文导出英文）。
- Telegram 内置浏览器部分 WebView 不实现 `window.print()`；点了没反应时，用 Telegram 菜单里的「在浏览器中打开」再导出。

### 导出 Excel（.xlsx）

- **纯手写生成**：不引第三方库、离线可用（自己实现了 OOXML 与存储式 ZIP + CRC32）。
- **4 个工作表**，与页面口径一致：
  | 工作表 | 列 | 末行 |
  |---|---|---|
  | 入账 | 时间 / 金额 / 标记 / 操作人 / 备注 | 小计（金额合计） |
  | 下发 | 时间 / 金额 / 手续费 / 净额 / 操作人 / 备注 | 小计（金额 / 手续费 / 净额合计） |
  | 分组 | 时间 / 代号 / 总入金额 / 总出金额 / 总账 | 合计 |
  | 总计 | 总进 / 总出 / 总账 + 群名 + 币种 + 期间 | — |
- **数字是真数字**（Excel 里可直接求和排序），文字用内联字符串，中文完好（已用 Python 解包校验过：ZIP 完整、4 个表、单元格值正确）。
- **已撤销记录不导出**，笔数写在「总计」表里；各表末行是「小计 / 合计」。
- 文件名形如 `账单明细_2026-09-15_20260923.xlsx`。
- 导出的是**当前筛选结果**（日期区间/时间生效中）；文案跟随语言。

## 七、鉴权与安全（未改动）

- 链接带 HMAC 签名：`?id=<base64url(user_id:chat_id:username)>&t=<签名>`，签名把身份与群绑定，改一个字符即失效。
- 只有管理员/操作员名单内的账号能打开；不在名单 → 403；签名无效 → 401。
- 页面本身不含任何数据，所有数据都通过 `/api/*` 带签名拉取；轮换 `WEB_CONSOLE_SECRET` 可吊销全部旧链接。
- 页面为**只读**：不提供任何写接口调用（`/api/ledger/add`、`/api/scans/confirm` 仍存在于服务端，但页面不再使用）。

---

## 八、接口与实现对照

| 前端行为 | 接口 | 后端实现 |
|---|---|---|
| 加载群名 | `GET /api/session` | `bot.py: _console_authorized / settings_view` |
| 加载三张表 | `GET /api/ledger?period=&start=&end=` | `bot.py: _console_period_view` |
| 分组表数据 | 同上返回的 `groups` | `bot.py: _console_group_rows` |
| 条目字段裁剪 | 同上返回的 `entries` | `bot.py: _console_entry_view` |
| 历史账期列表 | 同上返回的 `history_periods` | `bot.py: _console_periods` |

部署提醒：改动 `bot.py` / `webconsole.py` 后必须重新 `docker build` + `docker rm -f` + `docker run`，`restart` 不生效。
